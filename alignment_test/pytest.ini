[pytest]
markers =
    openai: OpenAI chat-completions spec conformance
    anthropic: Anthropic messages spec conformance
    streaming: streaming (SSE) behavior
    tools: tool / function calling
    errors: error-format and robustness conformance
    vision: image / multimodal input
addopts = -ra --tb=short
